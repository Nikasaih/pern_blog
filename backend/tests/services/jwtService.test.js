/* eslint-disable no-undef */
describe("jwtService", () => {
  it("test", () => {
    const a = 2;
    expect(a).toBe(2);
  });
  it("test", () => {
    const a = 4;
    expect(a).toBe(4);
  });
});
